// Copyright 2026 waveriderai and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newShortinterestPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagSymbols string
	var flagFinraReport bool
	var flagPage string
	var flagPageSize int
	var flagFrom string
	var flagTo string
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "shortinterest",
		Short: "Retrieves short interest data for specified securities.",
		Long:  "Retrieves short interest data for specified securities.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  benzinga-pp-cli shortinterest --symbols example-value",
		Annotations: map[string]string{"pp:endpoint": "shortinterest.get-short-interest", "pp:method": "GET", "pp:path": "/api/v1/shortinterest", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with a required flag/body prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only reads fall through so a bare call still executes; positional
			// commands keep their existing usageErr (exit 2 + JSON envelope).
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("symbols") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "symbols")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/v1/shortinterest"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "shortinterest", path, map[string]string{
				"symbols":     formatCLIParamValue(flagSymbols),
				"finraReport": formatCLIParamValue(flagFinraReport),
				"page":        formatCLIParamValue(flagPage),
				"pageSize":    formatCLIParamValue(flagPageSize),
				"from":        formatCLIParamValue(flagFrom),
				"to":          formatCLIParamValue(flagTo),
			}, nil, flagAll, "page", "page", "pageSize", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagSymbols, "symbols", "", "Comma-separated list of stock ticker symbols (e.g., AAPL,MSFT,TSLA). Required.")
	cmd.Flags().BoolVar(&flagFinraReport, "finra-report", false, "Include FINRA short interest report data. Default: false")
	cmd.Flags().StringVar(&flagPage, "page", "", "Page number for pagination. Used with pageSize parameter.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Number of results per page. Default varies by endpoint configuration.")
	cmd.Flags().StringVar(&flagFrom, "from", "", "Start date for short interest data. Format: YYYY-MM-DD")
	cmd.Flags().StringVar(&flagTo, "to", "", "End date for short interest data. Format: YYYY-MM-DD")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
