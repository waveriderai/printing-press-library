// Copyright 2026 waveriderai and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCalendarGetFdaCmd(flags *rootFlags) *cobra.Command {
	var flagPage string
	var flagParametersDate string
	var flagParametersDateFrom string
	var flagParametersDateTo string
	var flagParametersDateSearchField string
	var flagParametersDateSearchStrict bool
	var flagParametersSecurities string
	var flagParametersUpdated int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-fda",
		Short:       "Returns FDA approvals, clinical trials, and PDUFA (Prescription Drug User Fee Act)",
		Example:     "  benzinga-pp-cli calendar get-fda",
		Annotations: map[string]string{"pp:endpoint": "calendar.get-fda", "pp:method": "GET", "pp:path": "/api/v2.1/calendar/fda", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("parameters-date-search-field") {
				allowedParametersDateSearchField := []string{"announced", "target"}
				validParametersDateSearchField := false
				for _, v := range allowedParametersDateSearchField {
					if flagParametersDateSearchField == v {
						validParametersDateSearchField = true
						break
					}
				}
				if !validParametersDateSearchField {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagParametersDateSearchField, "parameters-date-search-field", allowedParametersDateSearchField)
				}
			}
			path := "/api/v2.1/calendar/fda"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "calendar", path, map[string]string{
				"page":                           formatCLIParamValue(flagPage),
				"parameters[date]":               formatCLIParamValue(flagParametersDate),
				"parameters[date_from]":          formatCLIParamValue(flagParametersDateFrom),
				"parameters[date_to]":            formatCLIParamValue(flagParametersDateTo),
				"parameters[date_search_field]":  formatCLIParamValue(flagParametersDateSearchField),
				"parameters[date_search_strict]": formatCLIParamValue(flagParametersDateSearchStrict),
				"parameters[securities]":         formatCLIParamValue(flagParametersSecurities),
				"parameters[updated]":            formatCLIParamValue(flagParametersUpdated),
			}, nil, flagAll, "page", "page", "pagesize", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagPage, "page", "0", "Page offset. For optimization, performance and technical reasons, page offsets are limited from 0 - 100000.")
	cmd.Flags().StringVar(&flagParametersDate, "parameters-date", "", "Date to query for calendar data. Shorthand for date_from and date_to if they are the same. Defaults for latest")
	cmd.Flags().StringVar(&flagParametersDateFrom, "parameters-date-from", "", "Date to query from point in time")
	cmd.Flags().StringVar(&flagParametersDateTo, "parameters-date-to", "", "Date to query to point in time")
	cmd.Flags().StringVar(&flagParametersDateSearchField, "parameters-date-search-field", "announced", "Date to filter and sort calendar by. Default is `announced` (one of: announced, target)")
	cmd.Flags().BoolVar(&flagParametersDateSearchStrict, "parameters-date-search-strict", true, "Some date fields (such as the FDA calendar target date) include fuzzy dates such as 2021-Q1.")
	cmd.Flags().StringVar(&flagParametersSecurities, "parameters-securities", "", "One or more security identifiers separated by a comma. Maximum 50 securities.")
	cmd.Flags().IntVar(&flagParametersUpdated, "parameters-updated", 0, "Records last Updated Unix timestamp (UTC).")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
