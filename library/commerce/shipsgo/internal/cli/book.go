// Copyright 2026 werkstoffclub and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelBookCmd(flags *rootFlags) *cobra.Command {
	var flagEtaWithin string
	var flagStatus string
	var flagTag string
	var flagPort string
	var flagCarrier string
	var flagMode string

	cmd := &cobra.Command{
		Use:         "book [filter...]",
		Short:       "Single view across ocean + air shipments, filterable by ETA window, status, tag, port, or carrier.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			fmt.Fprintln(cmd.OutOrStdout(), "This transcendence feature is planned for v0.2.")
			fmt.Fprintln(cmd.OutOrStdout(), "It requires a local SQLite shipment mirror that is not wired in v0.1.")
			fmt.Fprintln(cmd.OutOrStdout(), "Track progress: https://github.com/mvanhorn/printing-press-library/issues")
			return nil
		},
	}
	cmd.Flags().StringVar(&flagEtaWithin, "eta-within", "", "Only include shipments arriving within this window (e.g. 7d)")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Filter by exact status string (e.g. sailing, delivered)")
	cmd.Flags().StringVar(&flagTag, "tag", "", "Filter by tag substring")
	cmd.Flags().StringVar(&flagPort, "port", "", "Filter by port of loading or discharge")
	cmd.Flags().StringVar(&flagCarrier, "carrier", "", "Filter by carrier code (e.g. COSCO, MSC)")
	cmd.Flags().StringVar(&flagMode, "mode", "", `Limit to "ocean" or "air"`)
	return cmd
}
