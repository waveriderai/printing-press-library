// Copyright 2026 werkstoffclub and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelFollowersBroadcastCmd(flags *rootFlags) *cobra.Command {
	var remove bool
	cmd := &cobra.Command{
		Use:         "followers-broadcast [rfq-slug] [email]",
		Short:       "Add or remove one follower email across every shipment in an RFQ in one command, gated by credit-budget and --dry-run.",
		Annotations: map[string]string{"mcp:read-only": "false"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			action := "add"
			if remove {
				action = "remove"
			}
			fmt.Fprintf(cmd.OutOrStdout(), "This transcendence feature is planned for v0.2 (action=%s).\n", action)
			fmt.Fprintln(cmd.OutOrStdout(), "It requires a local SQLite shipment mirror that is not wired in v0.1.")
			fmt.Fprintln(cmd.OutOrStdout(), "Track progress: https://github.com/mvanhorn/printing-press-library/issues")
			return nil
		},
	}
	cmd.Flags().BoolVar(&remove, "remove", false, "Remove the follower instead of adding (default: add)")
	return cmd
}
