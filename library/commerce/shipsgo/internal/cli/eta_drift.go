// Copyright 2026 werkstoffclub and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelEtaDriftCmd(flags *rootFlags) *cobra.Command {
	var flagThreshold string
	var flagSince string

	cmd := &cobra.Command{
		Use:         "eta-drift [filter...]",
		Short:       "List shipments whose ETA shifted by more than N days since the last sync — sorted by drift magnitude.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			fmt.Fprintln(cmd.OutOrStdout(), "This transcendence feature is planned for v0.2.")
			fmt.Fprintln(cmd.OutOrStdout(), "It requires a local SQLite shipment mirror that is not wired in v0.1.")
			fmt.Fprintln(cmd.OutOrStdout(), "Track progress: https://github.com/mvanhorn/printing-press-library/issues")
			return nil
		},
	}
	cmd.Flags().StringVar(&flagThreshold, "threshold", "", "Minimum drift magnitude in days (e.g. 2)")
	cmd.Flags().StringVar(&flagSince, "since", "", "Limit ETA history to this lookback window (e.g. 7d, 24h)")
	return cmd
}
