// Copyright 2026 werkstoffclub and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

func newNovelWebhookTailCmd(flags *rootFlags) *cobra.Command {
	var flagSince string
	var flagShipment string
	var flagWatch bool

	cmd := &cobra.Command{
		Use:         "tail [filter...]",
		Short:       "Tail the locally stored webhook_events table chronologically; supports --since, --shipment, and --watch.",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if dryRunOK(flags) {
				return nil
			}
			fmt.Fprintln(cmd.OutOrStdout(), "This transcendence feature is planned for v0.2.")
			fmt.Fprintln(cmd.OutOrStdout(), "It requires a local SQLite shipment mirror that is not wired in v0.1.")
			fmt.Fprintln(cmd.OutOrStdout(), "Track progress: https://github.com/mvanhorn/printing-press-library/issues")
			return nil
		},
	}
	cmd.Flags().StringVar(&flagSince, "since", "", "Only events newer than this duration (e.g. 24h, 7d)")
	cmd.Flags().StringVar(&flagShipment, "shipment", "", "Limit to one shipment id")
	cmd.Flags().BoolVar(&flagWatch, "watch", false, "Continuously re-poll the event log (planned for v0.2)")
	return cmd
}
