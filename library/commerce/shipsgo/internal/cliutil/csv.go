// Copyright 2026 werkstoffclub and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cliutil

import "strings"

// SplitCSV converts a user-facing comma-separated flag value into JSON array
// items. Empty tokens are ignored so "a,,b" behaves like "a,b".
func SplitCSV(input string) []string {
	parts := strings.Split(input, ",")
	out := make([]string, 0, len(parts))
	for _, part := range parts {
		part = strings.TrimSpace(part)
		if part == "" {
			continue
		}
		out = append(out, part)
	}
	return out
}

// CSVTemplateObjects expands each CSV token into an object based on template.
// A string value exactly equal to "$value" is replaced by the current token.
func CSVTemplateObjects(input string, template map[string]any) []map[string]any {
	values := SplitCSV(input)
	out := make([]map[string]any, 0, len(values))
	for _, value := range values {
		expanded, ok := expandCSVTemplate(template, value).(map[string]any)
		if !ok {
			continue
		}
		out = append(out, expanded)
	}
	return out
}

func expandCSVTemplate(template any, value string) any {
	switch v := template.(type) {
	case map[string]any:
		out := make(map[string]any, len(v))
		for key, item := range v {
			out[key] = expandCSVTemplate(item, value)
		}
		return out
	case []any:
		out := make([]any, len(v))
		for i, item := range v {
			out[i] = expandCSVTemplate(item, value)
		}
		return out
	case string:
		if v == "$value" {
			return value
		}
		return v
	default:
		return v
	}
}
